<%= form_with(model: enrollment) do |form| %>
  <% if enrollment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(enrollment.errors.count, "error") %> prohibited this enrollment from being saved:</h2>

      <ul>
        <% enrollment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :student_id, style: "display: block" %>
    <%# form.text_field :student_id %>
    <%= form.collection_select :student_id, Student.all,:id,:id, include_blank: true %>
  </div>

  <div>
    <%= form.label :subject_id, style: "display: block" %>
    <%# form.text_field :subject_id %>
    <%= form.collection_select :subject_id, Subject.all,:id,:name, include_blank: true %>
  </div>

  <div>
    <%= form.label :date, style: "display: block" %>
    <%= form.date_field :date %>
  </div>
  <br>
  <div>
    <%= form.submit %>
  </div>
<% end %>
